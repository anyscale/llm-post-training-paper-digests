{
  "file": "code_SWE-agent_2405.15793.txt",
  "title": "SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering",
  "year": "2024",
  "category": "code",
  "data_curation": "Not a focus of this paper. The system assumes a fixed, pretrained LM and does no training, so there is no training-data pipeline. Evaluation uses existing datasets: SWE-bench (2,294 full test instances from 12 Python repos, a 300-instance Lite subset, and a 225-instance dev split) and HumanEvalFix (164 problems per language). A small hand-picked set of SWE-bench dev examples drives qualitative ACI iteration, and a random 37-instance dev subset is used for the hyperparameter sweep (temperature, window size, history). No generation, filtering, or de-duplication of training data is performed.",
  "curriculum": "Not a focus of this paper. There are no training stages, warmup/anneal schedules, or cross-stage data mixing; the LM weights are frozen. The only ordered composition is at inference time: a system prompt, an optional single in-context demonstration trajectory, and the per-turn ReAct loop of thought+action followed by environment feedback. Per-turn context management (collapsing observations older than the last five, retaining malformed-then-corrected turns) acts as a lightweight runtime curriculum over the conversation history rather than over model training.",
  "method": "The contribution is the Agent-Computer Interface (ACI): an LM-friendly abstraction layer over a Linux shell for software engineering, run with GPT-4 Turbo / Claude 3 Opus. Built on the ReAct thought-action loop, SWE-agent provides custom commands\u2014a 100-line file viewer (open/goto/scroll), an edit command that replaces a line range and re-displays the file, search/navigation (find_file/search_file/search_dir capped at 50 results), and submit. Key tricks: a flake8 linter guardrail rejects edits that introduce syntax/indentation/undefined-name errors (E999, E111-113, F821/822/831, E902), reverting them and showing before/after snippets to stymie cascading edit failures; observations older than the last five are collapsed to one line; format errors trigger retry then de-noising. The ACI is tuned by manual inspection and grid search on dev examples, without altering LM weights. It frames LM agents as a new class of end user whose interface should be co-designed with the environment, unifying tool use, prompting, and execution feedback.",
  "eval_metrics": "Benchmarks: SWE-bench full (2,294) and Lite (300), plus HumanEvalFix (164 per language). Primary metric is % Resolved / pass@1 (fraction of instances where all human-written unit tests pass after the model's patch is applied), with $ Avg. Cost per resolved instance (per-instance budget capped at $4) and pass@k for k up to 6. Headline numbers: SWE-agent w/ GPT-4 Turbo resolves 12.47% (286/2,294) of full SWE-bench and 18.00% of Lite, versus 3.8% for the prior best non-interactive RAG baseline and an 18.0 vs. 11.0 Shell-only result (a 64% relative gain). HumanEvalFix pass@1 is 87.7% Python, 89.7% JS, 87.9% Java. Claude 3 Opus reaches 10.46% on full SWE-bench; pass@6 on Lite is 35%. Ablations on Lite cover editor, search, viewer window size, and context management. File-localization F1 is 59.05% vs. 45.47% for BM25+Claude.",
  "key_results": "SWE-agent w/ GPT-4 Turbo resolves 12.47% (286/2,294) of full SWE-bench and 18.00% of SWE-bench Lite, far above the 3.8% prior best RAG baseline, and scores 87.7% pass@1 on HumanEvalFix-Python. With Claude 3 Opus it reaches 10.46% on full SWE-bench; pass@6 on Lite is 35%.",
  "evolution_note": "SWE-agent builds on the ReAct agent paradigm and the SWE-bench benchmark (Jimenez et al., 2024), transposing HCI-style interface-design principles to LM agents and arguing that tooling/interaction design, not weight changes, drives coding-agent gains. Released in 2024, it seeded the SWE-bench leaderboard ecosystem and motivated later work on agent-computer interfaces and agentic RL/fine-tuning over SWE-bench trajectories."
}
